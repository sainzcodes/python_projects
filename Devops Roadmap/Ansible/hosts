# This is an example of information formatting for Ansible. Information used is not real.

[webservers]
w1.ajsnetworking.com
w2.jsnetworking.com
192.168.1.100
192.168.[2-4].200

[dbservers]
db1.ajsnetworking.com
192.168.5.150


# Terminal interaction example

$ cd /etc/ansible
$ ls
$ vim ansible.cfg
$ q!
$ cd -
$ cd mystuff/
$ ls
$ vim sample.yml
$ ansible-playbook sample.yml


# To install ansible use in MACOS:

# Install Homebrew
$ /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)" 

# Install Ansible via Homebrew
$ sudo pip3 install ansible

# Verify that Ansible has been installed 
$ ansible --version

# Test connection to local hosts
$ ansible localhost -m ping